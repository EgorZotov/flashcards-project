import React from 'react';
import * as Styled from './styles';

const Sign = () => {
    return (
        <Styled.SignWrapper>
            <p>Sign page</p>
        </Styled.SignWrapper>
    );
};

export default Sign;
