import React from 'react';
import * as Styled from './styles';

const Sign = () => {
    const string = '123';
    return (
        <Styled.SignWrapper>
            <p>Sign page</p>
        </Styled.SignWrapper>
    );
};

export default Sign;
